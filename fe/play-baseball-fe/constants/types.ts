export interface User {
    id: number,
    email: string,
    nickname: string,
    role: string,
    createdAt: string,
    updatedAt: string,
    lastLoginDate: string,
    deletedAt: string,
    emailVerified: boolean
}